/*
 * Package with at least one class required for javac to compile classes which import this package
 * (which contain "import japidviews._tags.*"). These are Japid generated classes.
 * Javac cannot be configured to ignore this case (JDT compiler embedded in Play! is configured to ignore it).
 */
package japidviews._tags;

public class dummy
{
}