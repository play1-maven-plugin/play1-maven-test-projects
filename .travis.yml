language: java

cache:
  directories:
    - $HOME/.m2

install:
  - java -XX:+PrintFlagsFinal -version
  - mvn --version

script:
  - mvn verify -Dplay.seleniumSkip -Pautodeps,dist,play13,with-external-modules --update-snapshots --settings .travis.settings.xml -e

jdk: oraclejdk7
